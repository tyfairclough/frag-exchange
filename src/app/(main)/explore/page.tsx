export default function ExplorePage() {
  return (
    <div className="mx-auto flex w-full max-w-lg flex-1 flex-col gap-4 px-4 py-6">
      <h1 className="text-xl font-semibold text-base-content">Explore</h1>
      <p className="text-base leading-relaxed text-base-content/70">
        Listings and discovery will live here (Chunk 5). This screen exists to exercise navigation and the app shell.
      </p>
    </div>
  );
}
