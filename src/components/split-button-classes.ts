/**
 * Border radii for a two-segment primary split control (Daisy `join` + pill `btn`).
 * Inner seam is square; outer edges are fully rounded. Uses logical sides so the
 * group mirrors correctly in RTL.
 */
export const splitButtonPrimaryLeftSegmentClass = "!rounded-s-full !rounded-e-none";
export const splitButtonPrimaryRightSegmentClass = "!rounded-s-none !rounded-e-full";
